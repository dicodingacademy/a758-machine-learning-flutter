name: house_price_predictor_app
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ^3.13.1

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.5+1
  # todo-02-init-01: install dependencies
  intl: ^0.20.3
  tflite_flutter: ^0.12.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  # todo-01-init-01: download model, put the assets folder, and register it to pubspec.yaml
  assets:
    - assets/
